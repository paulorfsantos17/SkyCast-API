apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - rabbitmq-configmap.yaml
  - rabbitmq-statefulset.yaml        # ← NOVO
  - rabbitmq-headless-service.yaml   # ← NOVO
  # - rabbitmq-deployment.yaml
  - rabbitmq-service.yaml
  - rabbitmq-pv.yaml
  - rabbitmq-pvc.yaml